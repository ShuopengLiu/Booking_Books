
<h1>Shopping Cart</h1>

  <ul>
    <% cart.each do |book|%>
      <li>
        <%= book.title %> X <%= session[:shopping_cart][book.id.to_s]%> -- <%= link_to '[delete]', cart_path(book), method: :delete %>
      </li>
    <%end%>
  </ul>